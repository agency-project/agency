--- a/criu/tty.c
+++ b/criu/tty.c
@@ -2278 +2278,6 @@ static int tty_verify_ctty(void)
-			break;
+			/* The master can belong to a different process than the
+			 * slave and /dev/tty. Search all matching endpoints before
+			 * deciding that the controlling descriptor was inherited.
+			 */
+			if (p->pid_real == d->pid_real)
+				break;
